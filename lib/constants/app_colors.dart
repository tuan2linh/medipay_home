import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFFE53E3E); // Màu đỏ chính
  static const Color primaryLight = Color(0xFFFC8181); // Màu đỏ nhạt
  static const Color white = Color(0xFFFFFFFF);

  // Gray colors for inactive states
  static const Color gray = Color(0xFF718096);
  static const Color grayLight = Color(0xFFA0AEC0);
  static const Color grayDark = Color(0xFF4A5568);

  // Background colors
  static const Color background = Color(0xFFF7FAFC);
  static const Color surface = Color(0xFFFFFFFF);

  // Shadow color
  static const Color shadow = Color(0x1A000000);

  // Bottom nav bar specific colors
  static const Color navBarBackground = Color(0xFFFFFFFF);
  static const Color navBarShadow = Color(0x0A000000);
  static const Color activeColor = Color(0xFFE53E3E);
  static const Color inactiveColor = Color(0xFF718096);
  static const Color centerButtonBackground = Color(0xFFE53E3E);
}
